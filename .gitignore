node_modules/
dist/
.env
.env.local
*.log
.vercel
.env*
!.env.example

# Agent tooling scratch — session logs, skill caches, observation dumps.
# None of it describes the product; AGENTS.md is where durable notes live.
.agents/
.claude/
skill-observations/
skills-lock.json

# Session transcripts dumped by /export. Same class as the tooling above:
# a conversation log is not a project artefact.
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt

# graphify build artifacts (regenerate with /graphify .)
graphify-out/
.gstack/

# Codex session artefacts, written into the repo by the CLI.
.codex/
