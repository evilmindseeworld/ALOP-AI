# Road to 31 August 2026

Rewritten 2026-08-05, replacing the version written earlier the same day.
**26 days.** HEAD `556e89c`, CI green.

Fifteen commits since the first draft. Everything marked done below was
verified, not assumed — the verification is named next to each claim, because
"done" without a check is how §7 sat open while three handoffs called it
finished.

---

## Where it actually stands

| | |
|---|---|
| Frontend tests | **412** across 30 files |
| Backend tests | **282** |
| CI | green on `556e89c` and `1ac316c` |
| Council tools | **LIVE** (`COUNCIL_TOOLS=1`) |
| Shadow probe result | **7/7 requested tools, 0 unparsed** |
| CORS | host-parsed allowlist; attacker origins 403, real origins 200 |
| Rate limiting | live, User-Agent bypass closed |
| Body limits | 2 MB to `/api/chats` → 413 |
| Clerk | still **development** — capped at 100 users |

---

## Done since the first roadmap

- **CI** — three jobs: both suites, the build, and a parse of `server.js`.
- **SSE activity trail**, and a silent failure fixed on the way: a mid-stream
  error used to end the stream with no `[DONE]`, and the client then SAVED the
  empty accumulator as the answer.
- **`read_file` + upload path** — Postgres-backed, opaque ids, text formats
  only. PDF and docx deliberately refused.
- **Link validation** — dead and "no longer available" results are dropped
  before the council sees them. Verified live against a real 404.
- **Region detection** — country and currency from CDN header → timezone →
  `Accept-Language`. No geolocation, nothing stored, and no guess when there is
  no signal.
- **Cold start** — 22.5s measured; now overlapped with sign-in rather than
  following it.
- **Sign-in redesigned** around the council roster as the hero.
- **The shell**, the sakura frame, the grain layer, the composer seam.
- **Shadow mode** — and the probe it added is why enabling the loop was a
  decision rather than a leap.
- **Overlay tests** (16) and **dictation tests** (11). The overlay's duplicate
  SpeechRecognition implementation is gone; it had drifted to no ten-second
  ceiling on the one surface most likely to need it.
- **Dead code** — four unmounted components and `frontend/src/.env`.
- **Shared rate-limit store** — built, tested, dormant behind
  `RATE_LIMIT_STORE=postgres`.
- **Admin console** — no subprocess, four independent access conditions.
- **Domain cutover runbook** and an `origins` preflight command.

---

## Left to build

Ordered by what breaks worst if skipped.

### 1. Watch the live council for a day

`COUNCIL_TOOLS=1` went live today and has never run against real traffic. The
number to watch in `[TOOLS]` is **unique calls vs members**: seven members
producing seven unique calls every round means the dedupe is not earning its
place and the prompt needs to push harder toward shared phrasing. Also watch for
`no usable answers, falling back to the plain council` — that is the loop
failing quietly and costing three rounds to do it.

Nothing to write yet. This is a day of reading logs before deciding whether
anything needs changing.

### 2. `run_code` — the sandbox

The last piece of the tool-calling design. Ephemeral microVM, no network, no
environment, hard timeout. **Needs a vendor key that does not exist yet.**

`node:worker_threads` remains rejected: it shares the process, so the isolation
boundary would be V8's rather than the operating system's, and on the other side
of that boundary is a live Stripe secret key.

### 3. `CommandPalette` → `components/ui/command`

Still hand-rolled. Any swap has to keep its dropped-first-keystroke regression
test passing **unmodified** — that bug took a session to find.

### 4. Three duplicate top-level selectors

`*`, `.sidebar-rail`, and the `.chat-item:hover/.focus-within .chat-actions`
pair. Down from 16. All three are two declarations of one selector in one file,
not a cross-file override.

### 5. `SignInPage.css` is outside every guard

Imported directly rather than through the `App.css` manifest, so the cascade
snapshot never walks it, `cssHygiene` never counts its `!important`s, and
`zIndexOrder` never sees its bare `z-index: 300`. **That is why it drifted** —
it was still carrying a deleted wood-grain palette months after the app stopped
using one. It is correct now and nothing stops it drifting again.

### 6. Slice B (AI smartness), then D (sign-in polish)

---

## Blocked on the owner

1. **`ALLOWED_ORIGIN_SUFFIXES=-evilmindseeworlds-projects.vercel.app`** in
   Render. Without it, Vercel **preview** deployments cannot call the API —
   that is what "Failed to create chat" was. The production alias works either
   way.
2. **The domain.** Everything Clerk-related waits on it. Follow
   `docs/DOMAIN-CUTOVER.md`; it is ordered so nothing is briefly broken, and the
   `origins` console command verifies the result in one call.
3. **`TERMINAL_ADMINS` and `TERMINAL_SECRET`** — the admin console is disabled
   until both are set, and the boot log says so. Worth doing before the cutover,
   since `origins` is the preflight for it.
4. **Stripe prices.** Set in the Stripe dashboard, not in this codebase — the
   app reads `unit_amount` and `currency` from the API and formats them, and
   AED renders correctly (`AED 30`, `AED 280`). Confirm
   `STRIPE_PRICE_MONTHLY` / `STRIPE_PRICE_YEARLY` point at the AED prices, or
   `/api/billing/prices` 503s and the upgrade path hides itself.
5. **`GOOGLE_API_KEY`** — if unset, attaching an image returns a clear 503
   instead of vision.
6. **`migrations/004_rate_limits.sql`** whenever convenient. Nothing uses it
   until `RATE_LIMIT_STORE=postgres`, so it can land well ahead of need.
7. **`gh auth login`** — `gh pr create` still fails without it.
8. **Decide `alop-desktop`'s repo.** It has none, and Smart App Control blocks
   the Rust build on this machine, so the F9 overlay fix is unverified.

---

## Two things that will bite if forgotten

**Do not scale Render past one instance** without setting
`RATE_LIMIT_STORE=postgres` first. The in-memory store is per-process, so every
limit multiplies by the instance count, and scaling is a dropdown — no deploy,
no review, nothing that flags it. The boot log states which store is active.

**Read the cascade baseline diff before regenerating it.** Strip the element
indices first: a raw diff that does NOT shrink when indices are stripped means
something structural moved rather than renumbered. That check caught a stagger
this week that silently deleted the app's only shimmer and rendered identically
at rest.
